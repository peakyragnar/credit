# Runbook — per-company process

**Status: v0 (empty).** Runbook v1 is *written by* the Phase 0 Athene run — every step that works gets recorded here in executable detail; every step that breaks gets its fix recorded here. Stages per spec §5: Picture (L0) → Decompose (L1) → Drill (L2) → Link → Compute → Emit.

Nothing below this line yet.
